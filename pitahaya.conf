
{
  database => {
    host     => "db",
    schema   => $ENV{DB_SCHEMA},
    username => $ENV{DB_USERNAME},
    password => $ENV{DB_PASSWORD},
  },
  tmp_dir  => ($ENV{TMP_DIR} || "./tmp" ),
  data_dir => ($ENV{DATA_DIR} || "./data"),
  session  => {
    key => ($ENV{SESSION_KEY} || 'pitahaya.cms'),
  },
  export => {
    dir => ($ENV{EXPORT_DIR} || "./export"),
  },
  search => {
    sphinx => {
      host  => "sphinx",
      index => ($ENV{SPHINX_INDEX} || 'repositorio'),
    },
  },
}
